{
    "devices": ${DEVICE_NAMES},
    "thingGroup": "${THING_GROUP}",
    "region": "${REGION}",
    "roleAlias": "${SERVICE_ROLE_ALIAS}",
    "deploymentBucket": "${DEPLOYMENT_BUCKET}",
    "version": "${VERSION}"
}